import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  selectContainer: {
    gap: 5
  },
  selectItem: {
    paddingHorizontal: 10,
    paddingVertical: 15,
    borderRadius: 5,
    borderStyle: 'solid',
    borderWidth: 1
  }
});
